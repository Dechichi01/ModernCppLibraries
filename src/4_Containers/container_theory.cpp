/*
	. A container holds a sequence of objects (elements)
	. STL COntainers:
		. Sequence: vector, array, list,
		. Associative: map, set

	. An element in a container MUST obly to the following:
		. Copy/move operations
		. Swap operations
		. Compare operations
*/